Install

    virtualenv sandbox
    python setup.py install

Run
    cd /var/local/project/root
    manage runserver
    manage shell
    etc..


Deploy:

    tsocks fab deploy:target=production
    tsocks fab restart:target=production

Check status
    supervisorctl -c instance/supervisord.conf status
