Install

    virtualenv sandbox
    pip install -r requirements.txt

Run
    ./manage.py runserver


Deploy:

    tsocks fab deploy:target=production
    zope >>> /var/local/ecosys/sandbox/bin/supervisorctl -c /var/local/ecosys/instance/supervisord.conf restart ecosys
